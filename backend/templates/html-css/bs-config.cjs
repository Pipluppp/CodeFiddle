// This template now uses the `serve` CLI directly and no longer needs a BrowserSync
// configuration. The file is kept intentionally empty so downstream tooling that
// expects it continues to work without extra configuration.
